# Core
numpy
pandas
scipy
pyarrow                # Parquet artifacts for processed data
pyyaml
tqdm
requests

# ML / recommenders
scikit-learn
scikit-surprise        # SVD / SVD++ / KNN baselines + RMSE/MAE  (use Python 3.10/3.11)
implicit               # ALS for implicit/collaborative filtering
sentence-transformers  # Granite/MiniLM text embeddings for content-based model

# App + plots
streamlit
matplotlib
seaborn

# Dev
pytest
ruff                   # linter + formatter (quality gate)
pyright                # static type checker (quality gate)
pandas-stubs           # accurate pandas type stubs (clean pyright on DataFrame ops)
