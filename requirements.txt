# Core
numpy
pandas
scipy
pyarrow                # Parquet artifacts for processed data
pyyaml
tqdm
requests

# ML / recommenders
scikit-learn
scikit-surprise        # SVD / SVD++ / KNN baselines + RMSE/MAE  (use Python 3.10/3.11)
implicit               # ALS for implicit/collaborative filtering
sentence-transformers  # Granite/MiniLM text embeddings for content-based model
transformers           # HF sentiment pipeline for real runs (tests use FakeSentimentModel)
torch_geometric>=2.6,<3.0  # LightGCN + SAGEConv built-ins; CPU/MPS on darwin arm64

# Graph EDA + community/spectral analysis (feat/graph-eda-community)
networkx>=3.3,<4.0      # built-in louvain_communities, bipartite utilities

# Optional graph extras — install manually if you want Leiden / faster spectral:
#   pip install leidenalg python-igraph pyamg
# leidenalg / python-igraph: stronger modularity refinement than Louvain
# pyamg: enables eigen_solver='amg' for sklearn SpectralClustering on big sparse graphs

# App + plots
streamlit
matplotlib
seaborn
plotly

# Dev
pytest
ruff                   # linter + formatter (quality gate)
pyright                # static type checker (quality gate)
pandas-stubs           # accurate pandas type stubs (clean pyright on DataFrame ops)
